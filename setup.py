import setuptools

setuptools.setup(
    name='idsi_pkg',
    version='1.0.0',
    author='benoit.ipou@inphb.ci',
    description='Essai de création de package python',
    packages=setuptools.find_packages(),
    install_requires=[]
)